Pill-shaped label for tagging agenda items, circular topics, or document categories.

```jsx
<Tag tone="blue">Circulaire</Tag>
```

Tones map to the brand's logo accents (`navy`, `blue`, `orange`, `green`) plus a `neutral` default for low-emphasis labels. Use one tone per category consistently — don't assign colors arbitrarily.
