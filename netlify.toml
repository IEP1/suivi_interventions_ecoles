# Config Netlify — site 100% statique, aucune étape de build.
# Les données vivent dans Supabase (voir supabase/schema.sql, js/supabase-client.js) : Netlify ne
# sert ici qu'à héberger les fichiers, plus de fonction serveur ni de token à configurer.

[build]
  publish = "."
